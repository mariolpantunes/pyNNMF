[build-system]
requires = ['setuptools>=42']
build-backend = 'setuptools.build_meta'

[tool.pyright]
venvPath = "."
venv = "venv"

[tool.ruff]
line-length = 120

[tool.setuptools.package-data]
"pynnmf" = ["py.typed"]
